# Copilot addition: deployment prep - Environment configuration template

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=false

# Data Directories
ACCOUNTS_DIR=accounts
DATA_DIR=data
CONFIG_DIR=config

# Frontend Configuration
VITE_API_BASE=http://localhost:8000

# Model Configuration (optional overrides)
# WHISPER_MODEL=small
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# SPACY_MODEL=en_core_web_sm

# Search Configuration (optional overrides)
# FAISS_INDEX_PATH=data/search/index.faiss
# EMBEDDINGS_PATH=data/search/embeddings.jsonl

# Topic Configuration (optional overrides)
# MAX_UMBRELLAS=5
# SIMILARITY_THRESHOLD=0.7
# MMR_LAMBDA=0.7
